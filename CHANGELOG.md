# 更新日志

所有对用户可感知的变更记录在此。版本号遵循语义化版本。

## [1.1.1] - 2026-08-25

### 新增

**星运排盘（星座命理工作台）**
- 新增 `/astro/` 公开页：无需登录即可使用的免费星运工具，首页悬浮球新增「星运排盘」入口
- 单文件蓝图 `routes/astro.py` + 独立计算引擎 `services/astro_engine.py`；页面内容区改为独立于登录态渲染（游客可见），导航/悬浮球仍仅登录用户显示
- `/astro/api/*` 接口接入全局 JSON 错误处理与 CSRF 豁免白名单，附回归测试 `tests/test_astro.py`

**日历订阅（同步到手机日历）**
- 个人中心新增「日历订阅」卡片：生成含个人令牌的订阅链接 `/user/todo.ics?token=…`，iPhone / 华为 / 小米等手机日历添加一次即可自动同步待办（约 2 小时刷新）
- 默认同步**未来 3 天**未完成的待办：`?days=N` 可调整窗口；已完成的待办自动从手机日历消失，不再打扰
- 手机本地提醒按开始时间区分：**今天开始的待办提前 30 分钟提醒，未来几天的在截止前 1 小时提醒**；全天事件不附提醒
- 支持一键复制、「手机一键添加」(webcal) 与「重新生成」令牌，旧链接立即失效；订阅源为只读输出，不泄露任何可写权限
- 输出标准 iCal：自己创建 + 被指派的未完成待办；解析出的「全天」待办输出为全天事件；标题带分类前缀
- 小米日历(ical4j)兼容加固：空值属性行省略、METHOD:PUBLISH，任何一行不合规范导致的静默失败已规避
- 复用 `/api/token` 同一 API 令牌体系，令牌轮换同时作用于第三方接口鉴权

**智能待办解析修复**
- 会议通知类文本解析出的标题不足 6 字时（如【会议通知】），自动回退取「会议主题：」后的文字作为待办标题
- 「2026年8月25日（周二）08:40-10:00」类明确日期+括注星期不再因时刻已过被顺延一周；显式起止区间即使会议已开始/已结束也原样保留

## [v1.1.0] - 2026-08-23

本版本覆盖 2026-08-17 至 08-23 的全部变更。

### 新增

**首页工作台：时间轴 + 日历**
- 首页改为「日历 + 时间轴」双卡片一屏布局（PC 日历居左、时间轴居右，Pad 同为双栏）
- 时间轴分区简化为 8 个对称时间点：更早 / 过去一月 / 过去一周 / 今天 / 明天 / 未来一周 / 未来一月 / 更远，过去一个月看不到任务的问题根治
- 折叠式分区导航：时间点常驻轴上，滚动到位自动展开，点击标题折叠/展开；打开页面自动锚定今天，今天不足 3 条时沿未来方向级联展开
- 日历格子直接显示节假日名称与法定假日「休」/ 调休「班」角标；任务密度圆点数 = 当日实际待办数
- 点日期直接筛选时间轴（再点取消）；手机端点日期弹出底部列表；Pad/手机支持左右滑动切换月份
- PC 端时间轴卡片只保留「时间 + 标题 + 分类徽章」

**智能待办创建（自然语言解析）**
- 一句话创建待办：「开发登录功能 明天9点到下周五18点 @张三」，自动解析标题、起止时间、负责人、分类
- 新增「每天」「每周X」重复周期，原每 N 周/月/年保持
- jionlp 语言包启动预装 + 缺失自愈安装；未就绪时提示等待并暂停提交，不再静默返回错误时间

**待办回收站**
- 删除进入回收站，支持恢复与彻底删除

**随手记标签**
- 随手记弹窗新增标签输入（逗号分隔），保存成功后右上角轻提示并自动关闭

**暗黑模式**
- 顶栏月亮/太阳开关：手动切换 + 记忆选择 + 默认跟随系统，覆盖导航/搜索浮层/日历/弹层/抽屉/通知全部界面

**其他新增**
- 全局快捷键：n 新建 / j k 上下切换 / t 回今天（首页与待办页）
- PWA：静态资源 Service Worker 缓存，二次打开更快
- 知识库 API 令牌与文档级可见范围
- 剪切板粘贴内容自动检测待办

### 优化

**待办管理**
- 创建/编辑页：分配对象支持 @姓名搜索与常用人员星标；周期默认不重复
- 详情抽屉：按钮重排为 完成→编辑→废弃→删除 并移至标题下方；移除多余时间控件与重复进度条；null 内容兜底
- 编辑弹窗时间显示修复为 YYYY-MM-DD HH:MM 格式
- 待办详情面板：编辑/删除按钮移至分类、状态徽章右侧
- 分类计数徽章恒为 0 的问题修复（SQL 过滤顺序 + 脏数据归一化）

**移动端 / Pad**
- iPhone 底部安全区适配；输入框 16px 防 iOS 聚焦缩放；触控热区加大
- 手机端待办详情改滑出抽屉；笔记详情抽屉形态扩展到平板
- 发布栏紧凑化且不再遮挡时间轴
- 日历格子移动端高度自适应，节日名可读性提升

**性能与其他**
- 图标 CSS 异步化 + woff2 预载，消除渲染阻塞
- 辅助文字对比度达 WCAG AA；勾选乐观更新（即时反馈、失败回滚）
- 会话超时延长至 5 天；系统时间统一北京时间
- 统一检索点待办跳转列表页并高亮定位该条；顶栏检索框视觉收敛

### 修复

- 日历末行在部分窗口高度/缩放下被裁切（行高下限 + 亚像素取整吸收）
- 时间轴勾选框误触区与标题重叠、手机端点击标题无响应
- 日历普通休息日「休」角标丢失、中秋国庆只显名字不显休标
- 暗黑模式开关不生效（主题块转换遗漏）
- 手机端通知下拉超出屏幕、待办分类列表整块消失等多个 P0/P1 问题
- 过去任务渐进加载失效、级联展开在今天无待办时放弃展开

### 内部工程

- 单体 app.py 拆分为 core / kb / routes 包结构，配置收敛至 `.env`（`.env.example` 提供全清单）
- 测试回归套件建立：`python tests/run_tests.py`
- 容器启动加固：socket 清理、依赖安装日志落盘、jionlp 预装诊断

---

## [v1.0.0] 及更早

初始发布：任务协作审批流、随手记、知识库（OCR/知识点/图谱/问答）、数字分身、自动分类、统一检索、电子宠物、邮件提醒、通知中心、管理后台、体验登录与授权控制。
